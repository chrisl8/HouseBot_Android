package com.lofland.housebot;

/*
 * Like Command, but these are commands to for the Android side, like connect and speak.
 * Handy for things like the WebServer to send over commands.
 */

public enum AndroidCommand {
	CAMERA, SAY;
}
